{
  "watch": "src",
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./build.ts && tsc --build tsconfig.prod.json  && node ./dist/src",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
